component: button
url: http://localhost:5173/
test_steps:
- step: 1
  action: navigate
  instruction: Open the application
  target: http://localhost:5173/
- step: 2
  action: click
  instruction: Click the button to trigger its onClick handler
  selector: button
  expected: The onClick handler of the button is triggered
- step: 3
  action: verify
  instruction: Verify the expected outcome of the button click (e.g., a state change,
    a new element appearing, or a console log)
  expected: The application state or UI reflects the successful execution of the onClick
    function
